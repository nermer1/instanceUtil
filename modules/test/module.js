(function() {
    const options = {
        version: "1.0.0",
        require: ["strintuil"],
        util: {
            addArray: function() {
                        
            }
        }
    };
    Box.modules.put("ArrayUtil", options);
})();